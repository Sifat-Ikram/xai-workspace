export default function Footer() {
    return (
        <footer className="border-t border-border py-10 px-6 md:px-10">
            <div className="max-w-7xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-muted">
                <span className="font-mono">Xai — Intelligence Workspace</span>
                <span>Built as a frontend product prototype.</span>
            </div>
        </footer>
    );
}